 两阶段on-policy自提升微调
 一阶段off-policy sft：基于提纯后的蒸馏数据对internVL进行监督微调，初步建立视觉-物理属性映射能力
 二阶段on-policy sft：让微调模型自身对训练样本生成CoT推理和预测结果，再由Qwen-235B与GT联合对推理过程进行修正和优化；将修正后的高质量样本回流至训练集，继续微调模型，形成“生成-修正-重训”的迭代自提升闭环，有效缓解分布外退化。


11. “在线负样本挖掘”中，高误差预测作为新增负样本加入下一轮DPO。但这可能导致模型过度惩罚当前表现差的样本，反而忽略了全局分布。你有没有做过消融实验，对比在线挖掘vs随机负样本的效果？
好问题
12. Margin-based自适应惩罚：将MAPE映射为权重1+α·MAPE。α怎么确定的？如果MAPE很大（比如>100%），权重会超过2，是否会导致梯度爆炸？你提到loss clipping上限，这个上限值是如何设定的？有没有实验证明这种加权比简单的margin-based DPO（如固定margin）更优？
好问题
13. DPO迭代了3个epoch，每个epoch是重新采样8条回复还是复用之前的？如果复用，正负样本会固化；如果重新采样，计算开销很大。你们的实际做法是？另外，3个epoch后模型是否收敛（比如chosen与rejected的似然差距稳定）？
*好问题



2. CoT 的评判标准 ：CoT（思维链）里包含显式推理链（例如：“该商品是实木椅子，体积约为...，实木密度约为...，故重量为...”）。你们是如何对 Qwen 生成的推理链进行质量校验的？还是只要最终预测数值接近 GT，就盲目信任它的推理过程？
• 好问题
• 绝不能盲目信任“凑出来的正确答案”
3. InternVL 的架构局限 ：你用的是 InternVL 的哪个版本（如 1.5, 2.0-26B/76B）？InternVL 的 ViT 主要是在自然场景图像上预训练的。电商商品图往往包含大量文字贴纸、白底图、不规范裁剪，视觉模型如何准确提取出暗示物体“真实大小/材质”的微弱视觉特征？
好问题
4. 
5. 长尾商品的蒸馏崩溃 ：对于全新上架、没有同款检索 Few-shot、类目宽泛的商品，Qwen 生成的 CoT 质量会急剧下降。针对这部分严重缺乏先验知识的商品，你的蒸馏方案是如何防止模型摆烂的？
好问题
三、 两阶段 On-policy 自提升微调（13-18）
1. Off-policy 到 On-policy 的分布偏移 ：第一阶段结束后，InternVL 已经初步建立映射。第二阶段你让模型“自身生成结果”。此时模型生成的 CoT 往往是杂乱、格式不规范甚至包含严重逻辑错误的。这种情况下，Qwen-235B 怎么做到“对推理过程进行修正和优化”？Qwen 怎么知道模型哪一步看错了图？
好问题
2. 自提升的闭环流转代价 ：在“生成-修正-重训”的迭代中，由于 InternVL 模型在不断更新，你每一次迭代都需要对全量/增量训练集进行重新推理（On-policy 生成），这个显存和算力开销是巨大的。你们迭代了多少次？单次迭代的 ROI（投入产出比）高吗？
1. 好问题
3. 分布外（OOD）退化的悖论 ：你提到该机制“有效缓解分布外退化”。但 On-policy 本质上是在当前模型的策略分布内进行采样和修正，它怎么可能解决模型没见过的分布外（OOD）场景？
1. 好问题
4. 标签污染与过拟合 ：在第二阶段，修正后的样本反复回流进训练集。这极易导致模型对这批特定商品产生强烈的过拟合（记住了特定商品图和数字的对应关系），你们是如何在训练中做训练集和验证集隔离的？
1. 好问题
5. SFT 阶段的 Loss 设计 ：在训练 InternVL 时，你的 Loss 是只挂在最后的“物理属性数值 tokens”上，还是挂在整个“CoT 推理过程 + 数值 tokens”上？如果是后者，怎么防止模型为了凑字数生成无用的 CoT 从而拉低核心数值的准确率？
1. 好问题
6. 收敛性问题 ：在两阶段自提升过程中，有没有遇到过模型推理能力“坍塌”的情况（即模型学到了坏的推理模式，导致整体性能不升反降）？你们的停止迭代准则（Stopping Criterion）是什么？好问题
